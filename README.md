# IDPx-ShiftHappensProject
For IDPx course: Functional Prototype of a WebApp: Adulting101 with Flower

  ## START EVERYTHING: After saving the project to your machine JUST open the server.py file and click on run(play button) !!! or run directly in the terminal through navigating to the right folder structure and typing int the command: python server.py
  

  ## Running the code
  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  



